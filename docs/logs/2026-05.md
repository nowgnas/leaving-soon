# Work Log - 2026-05

## 2026-05-16

### Request Summary

User asked to create project-specific Codex documentation and workflow files for the currently opened Cursor project directory.

### Interpreted Task

Create a reusable project documentation structure that guides Codex to manage the project with `/client`, `/server`, and `/docs`, while tracking tasks and appending work logs after every request.

### Task

- Task ID: T-001
- Status: DONE

### Files Changed

- `AGENTS.md`
- `/client/README.md`
- `/server/README.md`
- `/docs/project/overview.md`
- `/docs/project/architecture.md`
- `/docs/project/conventions.md`
- `/docs/project/decisions.md`
- `/docs/tasks/task-board.md`
- `/docs/tasks/task-template.md`
- `/docs/logs/2026-05.md`
- `/docs/reviews/code-review.md`

### Decisions Made

- Use root `AGENTS.md` for project-specific Codex behavior.
- Use `/docs/tasks/task-board.md` for task tracking.
- Use `/docs/logs/YYYY-MM.md` for request-by-request work summaries.
- Keep `/client` and `/server` as strict top-level application boundaries.
- Keep framework initialization as a separate task.

### Verification

Docs-only change. Runtime verification is not required.

### Next Step

Fill `/docs/project/overview.md` with the actual service idea, target users, core features, and non-goals.

---

## 2026-05-16 23:40 KST

### Request Summary

User asked to build release1 as a working MVP based on `docs/plainning/planning_doc.md`, with Kakao map-based cafe search and a landing page that exposes two entry buttons: one for people going to cafes and one for people leaving cafes.

### Interpreted Task

Define and implement the release1 MVP for 곧나가요 as a locally runnable full-stack app with visitor and reporter flows.

### Task

- Task ID: T-005
- Status: DONE

### Files Changed

- `docs/plans/2026-05-16-release1-mvp.md`
- `docs/tasks/task-board.md`
- `docs/project/overview.md`
- `docs/project/architecture.md`
- `docs/project/conventions.md`
- `docs/project/decisions.md`
- `docs/logs/2026-05.md`
- `client/README.md`
- `client/index.html`
- `client/styles.css`
- `client/app.js`
- `server/README.md`
- `server/package.json`
- `server/src/domain.js`
- `server/src/app.js`
- `server/src/server.js`
- `server/test/domain.test.js`

### Decisions Made

- Use a no-build static client and a Node.js built-in HTTP server for release1.
- Store release1 reports in memory.
- Use Kakao Maps JavaScript API services `keywordSearch` with cafe category filtering for real cafe search.
- Provide sample cafe fallback when a Kakao JavaScript key is not configured.

### Verification

- `rtk node --test server/test/domain.test.js`
- `rtk node --check server/src/app.js`
- `rtk node --check client/app.js`
- `rtk node server/src/server.js`
- `rtk curl -s http://127.0.0.1:3000/api/health`
- `rtk curl -s http://127.0.0.1:3000/`
- `rtk curl -s -X POST http://127.0.0.1:3000/api/cafes/sample-seongsu-1/reports ...`
- `rtk curl -s http://127.0.0.1:3000/api/cafes/sample-seongsu-1/reports`

### Next Step

Add persistent storage and a production Kakao key/domain setup path for release2.

---

## 2026-05-16 23:52 KST

### Request Summary

User asked to choose client and server technology stacks, rebuild the project, and use shadcn for the frontend instead of arbitrary custom design.

### Interpreted Task

Replace the no-build release1 implementation with explicit stacks: Vite React TypeScript with shadcn/ui-style components for `/client`, and Fastify TypeScript with Zod/Vitest for `/server`.

### Task

- Task ID: T-006
- Status: DONE

### Files Changed

- `docs/plans/2026-05-16-release1-stack-rebuild.md`
- `docs/tasks/task-board.md`
- `docs/project/overview.md`
- `docs/project/architecture.md`
- `docs/project/conventions.md`
- `docs/project/decisions.md`
- `docs/logs/2026-05.md`
- `client/package.json`
- `client/package-lock.json`
- `client/index.html`
- `client/components.json`
- `client/tsconfig.json`
- `client/tsconfig.app.json`
- `client/tsconfig.node.json`
- `client/vite.config.ts`
- `client/src/App.tsx`
- `client/src/main.tsx`
- `client/src/index.css`
- `client/src/lib/utils.ts`
- `client/src/types/kakao.d.ts`
- `client/src/components/ui/*`
- `server/package.json`
- `server/package-lock.json`
- `server/tsconfig.json`
- `server/src/app.ts`
- `server/src/domain.ts`
- `server/src/server.ts`
- `server/src/types.ts`
- `server/test/domain.test.ts`

### Decisions Made

- Client stack: Vite, React, TypeScript, Tailwind CSS v4, shadcn/ui-style local components.
- Server stack: Fastify, TypeScript, Zod, Vitest, tsx.
- Keep Kakao Maps JavaScript cafe search in the client.
- Keep report storage in memory for release1.

### Verification

- `rtk npm install --no-audit --no-fund` in `/server`
- `rtk npm install --no-audit --no-fund` in `/client`
- `rtk npm test` in `/server`
- `rtk npm run build` in `/server`
- `rtk npm run build` in `/client`
- `rtk npm run start` in `/server`
- `rtk curl -s http://127.0.0.1:3000/api/health`
- `rtk curl -s -X POST http://127.0.0.1:3000/api/cafes/sample-seongsu-1/reports ...`

### Next Step

Run both dev servers together and do browser-level UI verification for the shadcn React client.

---

## 2026-05-17 00:28 KST

### Request Summary

User decided to keep Kakao Maps API and provided a Kakao JavaScript key. User also previously requested mobile-aware visual redesign and more color.

### Interpreted Task

Configure the Vite client to load Kakao Maps with a local environment key and refresh the shadcn-based UI for mobile proportions and stronger visual color.

### Task

- Task ID: T-007
- Status: DONE

### Files Changed

- `client/.env.local`
- `client/.env.example`
- `client/.gitignore`
- `client/README.md`
- `client/src/App.tsx`
- `client/src/index.css`
- `client/src/types/kakao.d.ts`
- `docs/project/architecture.md`
- `docs/project/decisions.md`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Keep Kakao Maps JavaScript API for cafe search.
- Load the Kakao key from `VITE_KAKAO_MAP_APP_KEY`, then `window.KAKAO_MAP_APP_KEY`, then local storage from a `kakaoKey` URL parameter.
- Store the provided local development key in `/client/.env.local`.
- Exclude `.env.local`, `node_modules`, and `dist` from `/client` git tracking.
- Use a mobile-first shadcn UI layout with Kakao yellow, mint green, coffee brown, and coral/destructive accents.

### Verification

- `rtk npm run build` in `/client`
- `rtk npm test` in `/server`
- `rtk npm run build` in `/server`
- `rtk npm run dev` in `/client`
- `rtk curl -s http://127.0.0.1:5173/`

### Next Step

Register the local Vite domain in Kakao Developers if the browser blocks SDK usage for the provided JavaScript key.

---

## 2026-05-17

### Request Summary

User asked whether the mobile landing title intentionally breaks between `곧` and `나가요`, and requested a one-line title if not intentional.

### Interpreted Task

Remove the forced mobile line break in the landing brand title and keep `곧나가요` on one line across mobile widths.

### Task

- Task ID: T-008
- Status: DONE

### Files Changed

- `client/src/App.tsx`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Remove the explicit mobile `<br>`.
- Add `whitespace-nowrap` and responsive title sizing so the brand title stays on one line.

### Verification

- `rtk npm run build` in `/client`

### Next Step

Check the landing page on a narrow mobile viewport and tune the exact title size if a specific device width still feels tight.

---

## 2026-05-17

### Request Summary

User provided a replacement Kakao JavaScript key and asked to use it.

### Interpreted Task

Replace the local Vite Kakao Maps JavaScript key used by the client.

### Task

- Task ID: T-009
- Status: DONE

### Files Changed

- `client/.env.local`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Keep the key only in `/client/.env.local`, which is excluded from git.

### Verification

- `rtk npm run build` in `/client`

### Next Step

Restart the Vite dev server so it reloads the updated environment variable.

---

## 2026-05-17 01:04 KST

### Request Summary

User decided to switch cafe lookup from Kakao to Naver because Naver supports localhost registration and platform-backed cafe lookup is preferable to maintaining a custom cafe search system.

### Interpreted Task

Replace client-side Kakao Maps SDK search with a server-side Naver Local Search API proxy while keeping the current seat report UX and fallback sample cafes.

### Task

- Task ID: T-010
- Status: DONE

### Files Changed

- `client/src/App.tsx`
- `client/src/types/kakao.d.ts`
- `client/README.md`
- `client/.env.local`
- `client/.env.example`
- `server/package.json`
- `server/package-lock.json`
- `server/.env.example`
- `server/.gitignore`
- `server/README.md`
- `server/src/app.ts`
- `server/src/naver.ts`
- `server/src/server.ts`
- `server/test/naver.test.ts`
- `server/test/domain.test.ts`
- `docs/project/overview.md`
- `docs/project/architecture.md`
- `docs/project/conventions.md`
- `docs/project/decisions.md`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Use Naver Local Search API through the Fastify server.
- Add `GET /api/cafes/search?query=...`.
- Keep Naver credentials on the server as `NAVER_CLIENT_ID` and `NAVER_CLIENT_SECRET`.
- Remove client-side Kakao SDK loading and local Kakao key files.
- Keep sample cafe fallback when Naver credentials are missing or search fails.

### Verification

- `rtk npm install --no-audit --no-fund` in `/server`
- `rtk npm test` in `/server`
- `rtk npm run build` in `/server`
- `rtk npm run build` in `/client`
- `rtk npm run start` in `/server`
- `rtk curl -s http://127.0.0.1:3000/api/health`
- `rtk curl -s 'http://127.0.0.1:3000/api/cafes/search?query=%EC%84%B1%EC%88%98%20%EC%B9%B4%ED%8E%98'`

### Next Step

Create `/server/.env` with Naver credentials, then restart the server and test live cafe search.

---

## 2026-05-17

### Request Summary

User provided Naver Client ID and Client Secret and asked to configure them.

### Interpreted Task

Create local server environment credentials for Naver Local Search and verify live cafe search.

### Task

- Task ID: T-011
- Status: DONE

### Files Changed

- `server/.env`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Store credentials only in `/server/.env`, which is ignored by `/server/.gitignore`.

### Verification

- `rtk npm run start` in `/server`
- `rtk curl -s http://127.0.0.1:3000/api/health`
- `rtk curl -s 'http://127.0.0.1:3000/api/cafes/search?query=%EC%84%B1%EC%88%98%20%EC%B9%B4%ED%8E%98'`
- `rtk npm test` in `/server`

### Next Step

Run both dev servers and confirm the client search UI displays live Naver cafe results.

---

## 2026-05-17 01:35 KST

### Request Summary

User provided a Gemini API key and asked to use Gemini for cafe report summaries.

### Interpreted Task

Add Gemini AI summary generation to the server while preserving the existing rule-based summary as a fallback.

### Task

- Task ID: T-012
- Status: DONE

### Files Changed

- `server/.env`
- `server/.env.example`
- `server/src/types.ts`
- `server/src/domain.ts`
- `server/src/summary.ts`
- `server/src/app.ts`
- `server/test/summary.test.ts`
- `docs/project/architecture.md`
- `docs/project/conventions.md`
- `docs/project/decisions.md`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Store `GEMINI_API_KEY` only in `/server/.env`.
- Use Gemini `gemini-2.5-flash` via `generateContent`.
- Disable Gemini thinking for short summaries with `thinkingBudget: 0`.
- Return `summary.source` as `ai` or `rule`.
- Fall back to rule summaries when Gemini fails or returns incomplete output.

### Verification

- `rtk npm test` in `/server`
- `rtk npm run build` in `/server`
- `rtk npm run build` in `/client`
- Direct Gemini REST smoke test with `thinkingBudget: 0`
- `rtk curl -s -X POST http://127.0.0.1:3000/api/cafes/ai-summary-test-3/reports ...`

### Next Step

Add persistence before deployment so AI summaries operate on durable report data instead of in-memory reports.

---

## 2026-05-17

### Request Summary

User reported that cafe lookup stopped working.

### Interpreted Task

Verify Naver cafe search through server and Vite proxy, then improve client-side feedback so search success and errors are visible.

### Task

- Task ID: T-013
- Status: DONE

### Files Changed

- `client/src/App.tsx`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Keep the Naver server proxy because both direct server and Vite-proxied search requests returned 200 with live Naver results.
- Add search loading and result/error messages inside the search card.
- Surface server error messages in the client when search fails.

### Verification

- `rtk curl -s -i http://127.0.0.1:3000/api/health`
- `rtk curl -s -i 'http://127.0.0.1:3000/api/cafes/search?query=%EB%A6%AC%EB%B8%8C%EB%A0%88%EB%A6%AC'`
- `rtk curl -s -i 'http://127.0.0.1:5173/api/cafes/search?query=%EB%A6%AC%EB%B8%8C%EB%A0%88%EB%A6%AC'`
- `rtk npm run build` in `/client`

### Next Step

Hard refresh the browser or restart the Vite dev server so the updated search feedback UI is loaded.

---

## 2026-05-17 10:32 KST

### Request Summary

User asked to hide the summary and recent reports on the `곧 나가요` page, make the select controls readable, and shift the overall visual language toward coffee and cafe tones.

### Interpreted Task

Refine the reporter page so it only shows the input form and selected cafe context, while tuning the visual system toward warm coffee colors and opaque form controls.

### Task

- Task ID: T-014
- Status: DONE

### Files Changed

- `client/src/App.tsx`
- `client/src/index.css`
- `client/src/components/ui/select.tsx`
- `client/src/components/ui/input.tsx`
- `client/src/components/ui/textarea.tsx`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Hide `자리 요약` and `최근 제보` entirely in `곧 나가요` mode.
- Keep reporter mode focused on the input form and selected cafe context.
- Replace transparent form controls with opaque coffee-toned controls.
- Shift the app palette to cream, mocha, caramel, and espresso accents.

### Verification

- `rtk npm run build` in `/client`

### Next Step

Check the reporter page on mobile and desktop to confirm the form feels legible and the coffee palette is balanced.

---

## 2026-05-17

### Request Summary

User asked to remove the remaining cafe report lookup from `곧 나가요` mode.

### Interpreted Task

Stop reporter mode from fetching cafe reports, leaving it as a write-only flow.

### Task

- Task ID: T-015
- Status: DONE

### Files Changed

- `client/src/App.tsx`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Reporter mode now clears selected state without calling `fetchCafeReports()`.
- Successful reporter submissions only show a confirmation message.

### Verification

- `rtk npm run build` in `/client`

### Next Step

If desired, we can simplify the reporter page further by hiding cafe details after selection and making it a pure submit flow.

---

## 2026-05-17 22:19 KST

### Request Summary

User asked to stop returning AI summaries when creating a seat report.

### Interpreted Task

Make `POST /api/cafes/:id/reports` persist the report and return only the created report, while leaving `GET /api/cafes/:id/reports` unchanged.

### Task

- Task ID: T-016
- Status: DONE

### Files Changed

- `server/src/app.ts`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- `POST /api/cafes/:id/reports` now returns only `{ report }`.
- `GET /api/cafes/:id/reports` still returns the AI or rule-based summary for visitors.

### Verification

- `rtk npm test` in `/server`
- `rtk npm run build` in `/server`
- `rtk npm run build` in `/client`
- `rtk curl -s -X POST http://127.0.0.1:3000/api/cafes/no-summary/reports ...`
- `rtk curl -s http://127.0.0.1:3000/api/cafes/no-summary/reports`

### Next Step

Consider whether reporter mode should also stop showing cafe details after a cafe is chosen, making it fully write-only.

---

## 2026-05-17 22:25 KST

### Request Summary

Verified the report registration response shape after removing summary data.

### Interpreted Task

Confirm that `POST /api/cafes/:id/reports` returns only the created report.

### Task

- Task ID: T-017
- Status: DONE

### Files Changed

- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Treat the response shape verification as a recorded documentation step.

### Verification

- `rtk curl -s -X POST http://127.0.0.1:3000/api/cafes/check-report-response/reports ...`
- Confirmed POST response contains only `report` and no `summary`.

### Next Step

No further change required for the report create response unless you want to remove more reporter-side detail.

---

## 2026-05-17 22:36 KST

### Request Summary

User reported that visitor mode no longer fetched summaries when clicking a cafe in `카페 갈래요`.

### Interpreted Task

Restore report lookup for visitor mode only, while keeping reporter mode write-only.

### Task

- Task ID: T-018
- Status: DONE

### Files Changed

- `client/src/App.tsx`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- `selectCafe()` now fetches reports only when `mode === "visitor"`.
- Reporter mode still skips report lookup.

### Verification

- `rtk npm run build` in `/client`

### Next Step

Check a cafe click in visitor mode to confirm the summary card is visible again.

---

## 2026-05-17 22:44 KST

### Request Summary

User asked how empty summary API responses are handled and requested a clearer no-report message when there is no stored data.

### Interpreted Task

Update the server-side summary fallback so an empty report set returns "아직 제보된 정보가 없어요." and verify the behavior with tests.

### Task

- Task ID: T-019
- Status: DONE

### Files Changed

- `server/src/domain.ts`
- `server/test/domain.test.ts`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Empty summary responses now use the exact message `아직 제보된 정보가 없어요.`
- The empty-case behavior is covered by a domain test so future changes do not drift.

### Verification

- `npm test -- test/domain.test.ts` in `/server`
- `npm run build` in `/server`

### Next Step

If you want, the visitor page can also show this message more prominently when the summary is empty.

---

## 2026-05-17 22:50 KST

### Request Summary

User asked to initialize git in the project root and commit all completed work.

### Interpreted Task

Create a git repository at `/Users/nowgnas/project/leaving-soon` and commit the current workspace snapshot without including ignored secret files.

### Task

- Task ID: T-020
- Status: DONE

### Files Changed

- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Initialized the repository at the project root with `git init`.
- Created a single snapshot commit for the tracked project files.
- Confirmed `.env` and `.env.local` files remain ignored by existing subdirectory gitignore rules.

### Verification

- `git status --short` after init to confirm only intended tracked files were present
- `git commit -m "Initial project snapshot"`

### Next Step

Continue normal feature work on top of commit `a9536d5`.

---

## 2026-05-17 23:00 KST

### Request Summary

User approved the persistence plan and asked to proceed with preserving recent report data and cleaning up stale records.

### Interpreted Task

Write the implementation plan for Supabase-backed persistence and cleanup, then prepare the repository for the actual rollout.

### Task

- Task ID: T-021
- Status: DONE
- Follow-up Task ID: T-022
- Status: IN_PROGRESS

### Files Changed

- `docs/plans/2026-05-17-supabase-persistence-cleanup.md`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Chose Supabase Postgres as the durable store for seat reports.
- Defined `active` reports as rows whose `expires_at` is still in the future.
- Chose a 24-hour post-expiration retention window before hard deletion.
- Chose Supabase Cron as the preferred cleanup scheduler, with GitHub Actions as fallback.

### Verification

- Documentation-only change; no runtime verification required yet.

### Next Step

Implement the repository, cleanup helper, and DB-backed read/write paths in the server.

---

## 2026-05-17 23:35 KST

### Request Summary

User approved the persistence rollout and asked to proceed with the actual implementation.

### Interpreted Task

Replace in-memory report storage with Supabase-backed persistence, add stale-report cleanup helpers, and document the new deployment flow.

### Task

- Task ID: T-022
- Status: DONE

### Files Changed

- `server/src/app.ts`
- `server/src/repository.ts`
- `server/src/cleanup.ts`
- `server/test/repository.test.ts`
- `server/test/cleanup.test.ts`
- `server/.env.example`
- `server/README.md`
- `docs/project/architecture.md`
- `docs/project/overview.md`
- `docs/project/decisions.md`
- `docs/tasks/task-board.md`
- `docs/plans/2026-05-17-supabase-persistence-cleanup.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Server now uses Supabase Postgres when `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` are present.
- Local development still falls back to in-memory storage.
- Cleanup uses a 24-hour post-expiration retention window.
- Cleanup can run through `POST /api/internal/cleanup/reports` with `CLEANUP_SECRET`.

### Verification

- `npm test` in `/server`
- `npm run build` in `/server`

### Next Step

Create the Supabase table and configure the deployment secrets, then wire a cron job to the cleanup endpoint or run the delete SQL directly from Supabase Cron.

---

## 2026-05-17 23:42 KST

### Request Summary

User asked for a deployment strategy summary including the platform choices before deployment.

### Interpreted Task

Document the recommended low-cost deployment path, call out required environment variables and operational prerequisites, and record the architecture decision.

### Task

- Task ID: T-023
- Status: DONE

### Files Changed

- `docs/project/decisions.md`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Recommended deployment path: Cloudflare Pages for the client, Render Free for the server, and Supabase Free for persistence.
- Cleanup should be handled by Supabase Cron or a scheduled call to the cleanup endpoint.
- The client will need a production API base URL; the server will need Supabase and cleanup secrets.

### Verification

- Documentation-only change; no runtime verification required.

### Next Step

Prepare the Supabase table, then wire the deployment environment variables and client API base URL before publishing.

---

## 2026-05-18 21:33 KST

### Request Summary

User asked to turn off local servers running on ports 5173 and 4173.

### Interpreted Task

Find local processes listening on TCP ports 5173 and 4173, stop them, and verify both ports are free.

### Task

- Task ID: T-024
- Status: DONE

### Files Changed

- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Treat this as a local chore task because no application code or configuration needed to change.
- Stop the `node` process listening on `127.0.0.1:5173`.

### Verification

- `lsof -nP -iTCP:5173 -sTCP:LISTEN`
- `lsof -nP -iTCP:4173 -sTCP:LISTEN`

Both verification commands returned no listening processes.

### Next Step

Restart the client or server dev process only when local testing is needed again.

---

## 2026-05-18 22:03 KST

### Request Summary

User said the current service design was unsatisfactory and asked to revise it.

### Interpreted Task

Refresh the client UI while preserving the current visitor and reporter flows.

### Task

- Task ID: T-025
- Status: DONE

### Files Changed

- `client/src/App.tsx`
- `client/src/index.css`
- `docs/plans/2026-05-18-service-design-refresh-design.md`
- `docs/plans/2026-05-18-service-design-refresh.md`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Combine mobile app-like simplicity with warm cafe brand expression.
- Keep the client as a single React app and avoid backend or API changes.
- Make the landing screen an app home with two clear actions.
- Improve visitor and reporter screens with clearer search, cafe selection, summary metrics, and form sections.

### Verification

- `npm run build` in `/client`

### Next Step

Review the refreshed UI in the local dev server and tune spacing, copy, or color details based on what feels off in the browser.

---

## 2026-05-18 22:34 KST

### Request Summary

User objected to the landing page helper information and asked whether the color palette should change.

### Interpreted Task

Remove the confusing landing helper blocks and adjust the palette away from the previous coffee/amber-heavy direction.

### Task

- Task ID: T-026
- Status: DONE

### Files Changed

- `client/src/App.tsx`
- `client/src/index.css`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Remove the landing page `확인 / 제보 / 정보` helper strip.
- Remove the extra bottom helper sentence from the landing page.
- Shift the visual palette to ink blue, cool mint, and coral accents while preserving existing component structure.

### Verification

- `npm run build` in `/client`

### Next Step

Review the simplified landing page in the browser and decide whether the new mint/coral palette is the right direction.

---

## 2026-05-18 22:47 KST

### Request Summary

User asked to remove the small `찾기` and `제보` labels from the landing page action cards, leaving only `카페 갈래요` and `곧 나가요`, while keeping the current colors.

### Interpreted Task

Simplify the landing action cards and preserve button affordance without extra text labels.

### Task

- Task ID: T-027
- Status: DONE

### Files Changed

- `client/src/App.tsx`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Keep the current mint/coral palette unchanged.
- Remove the small `찾기` and `제보` text badges.
- Add circular arrow affordances, hover elevation, and focus rings so the cards still read as clickable buttons.

### Verification

- `npm run build` in `/client`

### Next Step

Review the landing page interaction in the browser and adjust card spacing or icon treatment if needed.

---

## 2026-05-18 23:11 KST

### Request Summary

User suspected cafe lookup was failing because Korean cafe names were encoded in cafe IDs, and asked whether cafe IDs could be stored without cafe names.

### Interpreted Task

Change Naver cafe ID normalization so cafe IDs use only Naver coordinate data and no Korean cafe name.

### Task

- Task ID: T-029
- Status: DONE

### Files Changed

- `server/src/naver.ts`
- `server/test/naver.test.ts`
- `docs/tasks/task-board.md`
- `docs/logs/2026-05.md`

### Decisions Made

- Generate Naver cafe IDs as `naver-{mapx}-{mapy}` instead of `naver-{mapx}-{mapy}-{name}`.
- Keep `place_name` unchanged for UI display and report payloads.
- Existing short-lived reports saved with the old name-including IDs may not match the new ID, but new reports and lookups use the stable name-free ID.

### Verification

- `npm test -- test/naver.test.ts` in `/server`
- `npm test` in `/server`
- `npm run build` in `/server`

### Next Step

Deploy the server change, then submit and read a report for the same Korean-named cafe to confirm lookup stability in production.
